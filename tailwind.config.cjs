const config = {
  content: ["./src/**/*.{html,js,svelte,ts}"],

  theme: {
    extend: {
      colors: {
        "poke-color": "#d53846",
      },
    },
  },

  plugins: [],
};

module.exports = config;
